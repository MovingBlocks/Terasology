/**
 * Almost guaranteed to never stab or speak with you. Otherwise a fairly addon basic block
 * Note that this version sits outside the game as a prototype for external blocks -
 * but a clone exists internally as well as it is too neat to lack for now
 * Eventually the internal version should be whacked when this version can be imported
 */
block {
    // Serialization
    version = 1
}